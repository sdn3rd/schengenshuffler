{
  "name": "schengenerator",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:worker": "npm --prefix frontend run build && npx esbuild worker/index.js --bundle --format=esm --platform=neutral --outfile=frontend/dist/_worker.js && wrangler pages dev frontend/dist",
    "build": "npm --prefix frontend run build && npx esbuild worker/index.js --bundle --format=esm --platform=neutral --outfile=frontend/dist/_worker.js",
    "deploy": "CLOUDFLARE_ACCOUNT_ID=1958a44c637f9061a6cf574141164146 npm run build && wrangler pages deploy frontend/dist --project-name schengenerator",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "esbuild": "^0.28.0",
    "wrangler": "^3"
  }
}
